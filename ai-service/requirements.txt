# Versions épinglées à l'exact : le classifieur est réentraîné à chaque démarrage du
# service (app.py) plutôt que chargé depuis un artefact binaire, donc la version de
# scikit-learn fait partie du comportement observable du module IA - pas seulement de son
# build. Sans épinglage, deux `docker compose build` à trois mois d'écart pouvaient
# produire deux classifieurs différents sans qu'une ligne du dépôt ait changé, ce qui rend
# les mesures d'EVALUATION.md invérifiables (§12.3 - reproductibilité de l'évaluation).
Flask==3.1.3
scikit-learn==1.9.0

# Serveur WSGI de production (§15.1) - le serveur de développement de Flask avertit
# lui-même à chaque démarrage qu'il ne doit pas être utilisé pour servir.
gunicorn==23.0.0
